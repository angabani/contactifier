import type { ContactWriterCertification } from '@/application';

// Production native writing remains unavailable until a platform certification is added here.
export const nativeContactWriterCertifications: readonly ContactWriterCertification[] =
  Object.freeze([]);
